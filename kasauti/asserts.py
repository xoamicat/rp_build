"""Custom promptfoo assertions (drop 4). Placeholder so the config validates."""


def check_money(output: str, context: dict) -> dict:
    return {"pass": True, "score": 1.0, "reason": "placeholder until drop 4"}
